---
title: "Paso 2: Implementar GET /equipos"
labels: "auto-issue"
---
Debes implementar el endpoint `GET /equipos` en `server.js`.
Utiliza `collection.find()` para obtener todos los equipos, conviértelos a un arreglo con `toArray()` y devuélvelos en formato JSON con status `200`.
